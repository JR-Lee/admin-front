import storage from './storage'
import formatDate from './formatDate'
import fullScreen from './fullScreen'
import toBase64 from './toBase64'
import { password, mail, required } from './validate'

export {
  storage,
  formatDate,
  fullScreen,
  toBase64,
  password,
  mail,
  required
}